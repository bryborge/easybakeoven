# Config Template

At minimum, do the following things if defining a new services from this
template:

- Update the image name in `config/template.cfg`
- Rename `profile/template`
- Rename and customize `my.options`
